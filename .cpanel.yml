deployment:
  tasks:
    - export REPOPATH=/home/dzartsiomi/repo/*
    - export SITEPATH=/home/dzartsiomi/public_html/
    - /bin/rsync -rav --exclude '.git' --exclude '.cpanel.yml' --del $REPOPATH $SITEPATH